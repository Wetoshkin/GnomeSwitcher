# GnomeSwitcher

**GnomeSwitcher** — это легковесный аналог Punto Switcher для Linux (GNOME), специально разработанный для работы в среде **Wayland**. 

Проект позволяет автоматически или вручную исправлять текст, набранный в неверной раскладке, мгновенно переключая системный язык и перепечатывая последнее слово.

## Основные возможности

- ⌨️ **Перехват на уровне ядра**: Использование `evdev` позволяет обходить ограничения безопасности Wayland и работать во всех приложениях (браузеры, терминалы, IDE).
- 🔄 **Умное исправление**: По нажатию хоткея (по умолчанию F12) программа стирает последнее слово и вводит его заново в другой раскладке.
- 🖼️ **Современный GUI**: Настройки выполнены на GTK4 и Libadwaita, идеально вписываясь в интерфейс Ubuntu и Fedora.
- 🛠️ **Интеграция с GNOME**: Прямое управление раскладкой через D-Bus/gsettings, что обеспечивает мгновенное обновление иконки языка в системном трее.
- 🚀 **Автозапуск**: В комплекте скрипт для настройки автоматического старта при входе в систему.

## Системные требования

- **ОС**: Ubuntu 24.04+, Fedora 40+ (или любой дистрибутив с GNOME 45+).
- **Протокол**: Wayland или X11.
- **Python**: 3.10 или новее.

## Установка

1. **Установите системные зависимости**:
   ```bash
   sudo apt install python3-evdev python3-gi gir1.2-gtk-4.0 gir1.2-adw-1 libadwaita-1-dev
   ```

2. **Настройте права доступа**:
   Для работы с клавиатурой без прав root добавьте своего пользователя в группу `input`:
   ```bash
   sudo usermod -aG input $USER
   ```
   *Важно: после этой команды нужно перезагрузить компьютер или выйти из сессии.*

3. **Склонируйте репозиторий и запустите установку**:
   ```bash
   git clone https://github.com/vash-akkount/gnome-switcher.git
   cd gnome-switcher
   chmod +x install.sh
   ./install.sh
   ```

## Как это работает

В отличие от классических программ для X11, в Wayland приложения не могут «подслушивать» друг друга. GnomeSwitcher решает эту проблему, читая данные напрямую из устройства `/dev/input/eventX`. 

При нажатии клавиши исправления:
1. Демон посылает серию нажатий `Backspace` через виртуальное устройство `uinput`.
2. Команда `gsettings` переключает активную раскладку в GNOME.
3. Демон «проигрывает» скан-коды последнего слова заново, и система интерпретирует их уже в новой раскладке.

## Разработка

Проект состоит из двух частей:
- `daemon.py`: Фоновый процесс для работы с клавиатурой.
- `gui.py`: Окно настроек.

## Лицензия

MIT
